<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.braintreepayments.samsungdxtest"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="debug_mode" android:value="Y" />
        <meta-data android:name="spay_debug_api_key" android:value="e0f17c5c31a24c7db255c02f8bbada04" />
        <meta-data android:name="spay_sdk_api_level" android:value="1.9" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>